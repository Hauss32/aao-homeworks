<% 
does_follow = current_user.follows?(user) ? 'followed' : 'unfollowed'
%>

<button 
  class="follow-toggle"
  data-user-id="<%= user.id %>"
  data-initial-follow-state="<%= does_follow %>">
</button>

